# Project asset notes

- `../favicon.ico` — project-local procedurally generated geometric travel mark;
  no external image asset is used.

Editor-only preview images under `assets/editor/` are development resources and
are excluded from the standard player-facing release bundle.
